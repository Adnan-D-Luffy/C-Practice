#include <stdio.h>
#include <stdbool.h>

int main () {
    // Bool contains true or false
    bool a = true;
    bool b = false ;

    printf("%d \n" ,b);
    if (a) {
        printf("You are a good boy");
    }
    else {
        printf("You are not a good boy .");
    }
    return 0;
}